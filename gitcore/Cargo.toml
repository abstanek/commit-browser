[package]
name = "gitcore"
version = "0.1.0"
edition = "2024"

[dependencies]
base64 = "0.22"
git2 = { version = "0.21", default-features = false }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
